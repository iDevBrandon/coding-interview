const input1 = document.getElementById("num1")! as HTMLInputElement;

// "!" we are sure that this will never be null
// Always input element

function add(num1: number, num2: number) {
  return num1 + num2;
}
